The .exe's provided in this directory are built with microsoft visual c++ compiler and
these can be run in command line mode with password for file as second argument
these can also be used with 'open with' option in windows OS.

TO Use for open with Option
just drag and drop the input file onto exe
you can also set default association of huuf files with uncompresser exe

Enjoy Using File Genie 0.97